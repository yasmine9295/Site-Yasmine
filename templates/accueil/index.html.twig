<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YM{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🤍</text></svg>">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <script src="https://kit.fontawesome.com/2bcd4f9bb1.js" crossorigin="anonymous"></script>

    {% block stylesheets %}
    <style>


        /* Global styles */
        body {
            font-family: 'Helvetica', sans-serif;
            background-color: #fff;
            margin: 0;
            padding: 0;
            overflow: hidden; /* Prevent scrolling to ensure the background video stays in place */
        }
<<<<<<< HEAD
    
        /* Navbar styles */
        .navbar {
            display: flex;
            align-items: center;
            padding: 15px 30px;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background-color: #fff;
            padding: 10px 30px; /* Reduced padding for a smaller navbar */
            z-index: 1000;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-size: 1.6rem;
            font-weight: bold;
            color: #222;
            text-decoration: none;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .navbar-nav {
            list-style-type: none;
            display: flex;
            margin-left: auto;
        }

        .nav-item {
            margin-right: 20px;
        }

       
        .nav-link {
            color: #555;
            font-size: 1rem;
            text-transform: uppercase;
            font-weight: 500;
            transition: color 0.3s ease, transform 0.3s ease;
            text-decoration: none;
        }

        .nav-link:hover {
            color: #333;
            transform: translateY(-2px);
        }
=======
>>>>>>> 1124cf96439a588eb1d5c7638f7f5abc05e92a67

        /* Background video section */
        .background-video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* Ensure video stays in the background */
            overflow: hidden;
        }

        #background-video {
            width: 100%;
            height: 100%;
            object-fit: cover; /* Make sure the video covers the full screen */
        }

        .main-section {
            position: relative;
            height: 100vh; /* Full viewport height */
        }

        /* Each video section (left and right) */
        .video-section {
            flex: 1;
            position: relative;
            overflow: hidden; /* Ensure videos stay within their containers */
        }

        /* Style for the video iframe */
        .video-container {
            width: 100%;
            height: 100%;
            border: none;
            object-fit: cover; /* Ensure the video fills the section */
        }

        /* Ensure that the video fills the container */
        .video-container {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Optionally, you can add content text on top of the video */
        .content-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: white;
            text-align: center;
            z-index: 2;
        }

        .content-text {
            z-index: 1;
            text-align: center;
             color: white;
            padding: 20px;
        }

        .content-text h1 {
            font-size: 3rem;
            font-weight: bold;
        }

        .content-text p {
            font-size: 1.2rem;
        }

        .btn {

            color: #f1f1f1;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

                /* Style pour le bouton élégant */
        .btn-elegant {
            padding: 15px 40px; /* Plus de padding pour rendre le bouton plus grand */
            font-size: 1.5rem; /* Taille de texte plus grande */
            font-weight: 700; /* Texte plus gras pour l'élégance */
            background-color: #090706; /* Fond sombre élégant */
            color: #fff; /* Texte en blanc */
            border: 2px solid transparent; /* Bordure invisible */
            border-radius: 50px; /* Bordure arrondie pour un aspect moderne */
            text-transform: uppercase; /* Majuscules pour l'aspect luxueux */
            letter-spacing: 2px; /* Espacement des lettres pour plus de style */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Ombre douce autour du bouton */
            transition: all 0.4s ease; /* Transition fluide pour les effets de survol */
            display: inline-block; /* S'assurer que le bouton se comporte comme un élément en ligne */
        }

        /* Effet de survol élégant */
        .btn-elegant:hover {
            background-color: #34495e; /* Changement de couleur lors du survol */
            transform: translateY(-5px); /* Légère élévation lors du survol */
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25); /* Ombre plus marquée */
            color: #fff; /* Garder le texte en blanc au survol */
        }

        /* Effet de focus pour l'accessibilité */
        .btn-elegant:focus {
            outline: none; /* Enlever le contour par défaut */
            border-color: #fff; /* Bordure blanche lors du focus */
        }
    </style>
    {% endblock %}

</head>

<body>
<!-- Main section for videos -->
<div class="main-section">
    <!-- First video section (left) -->
    <div class="video-section">
        <div class="content-text"> 
            <h1>Notre Présentation</h1>
            <p>Plongez dans l'univers de Y&M </p>
            <p>où l'élégance rencontre la mode.</p>
            <a href="{{ path('admin_defiles') }}" class="btn">Découvrir</a>
        </div>
</div>
    <!-- Second video section (right) -->
    <div class="video-section">
        <div class="content-text">
            <h1>Bienvenue dans le monde du luxe</h1>
            <p>Découvrez nos dernières collections et défilés</p>
            <a href="{{ path('admin_blog') }}" class="btn">Explorez le Blog</a>
        </div>
    </div>
</div>

<!-- Other content -->
<div class="container">
    {% block body %}{% endblock %}
</div>

{% block javascript %}{% endblock %}

</body>
</html>
