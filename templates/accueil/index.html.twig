<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YM{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>☾★</text></svg>">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <script src="https://kit.fontawesome.com/2bcd4f9bb1.js" crossorigin="anonymous"></script>

    {% block stylesheets %}
    <style>
        /* Global styles */
        body {
            font-family: 'Helvetica', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        /* Navbar styles */
        .navbar {
            background-color: #fff;
            padding: 15px 30px;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-size: 2rem;
            font-weight: bold;
            color: #222;
            text-decoration: none;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .navbar-nav {
            list-style-type: none;
            display: flex;
            margin-left: auto;
        }

        .nav-item {
            margin-right: 20px;
        }

        .nav-link {
            color: #555;
            font-size: 1.1rem;
            text-transform: uppercase;
            font-weight: 500;
            transition: color 0.3s ease, transform 0.3s ease;
            text-decoration: none;
        }

        .nav-link:hover {
            color: #333;
            transform: translateY(-2px);
        }

        /* Main section with video */
        .main-section {
            display: flex;
            height: 100vh;
            background-color: #f0f0f0;
        }

        .left-section {
            width: 50%;
            background-color: #222;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .right-section {
            width: 50%;
            background-color: #f1f1f1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .video-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1; /* To keep the text on top */
            filter: blur(5px);
        }

        .content-text {
            z-index: 1;
            text-align: center;
            padding: 20px;
        }

        .content-text h1 {
            font-size: 3rem;
            font-weight: bold;
        }

        .content-text p {
            font-size: 1.2rem;
        }

        .btn {
            background-color: #f1f1f1;
            color: #333;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            text-decoration: none;
        }

        .btn:hover {
            background-color: #f1f1f1;
            transform: translateY(-2px);
        }
    </style>
    {% endblock %}
</head>
<body>

<!-- Navbar -->
<nav class="navbar">
    <a href="{{ path('accueil') }}" class="navbar-brand">
        <img src="{{ asset('images/logo.png') }}" alt="Y&M Logo" class="navbar-logo">
    </a>
    <div class="navbar-menu">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_defiles') }}">Défilés</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_marque') }}">Marques</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_mannequins') }}">Mannequins</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_blogs') }}">Partie Blog</a></li>
        </ul>
    </div>
</nav>

<!-- Main content area with video background -->
<div class="main-section">
    <!-- Left section with video background -->
    <div class="left-section">
        <video autoplay muted loop class="video-container">
            <source src="https://www.example.com/video.mp4" type="video/mp4"> 
            Your browser does not support the video tag.
        </video>
        <div class="content-text">
            <h1>Notre Présentation</h1>
            <p>Plongez dans l'univers de Y&M </p>
            <p>où l'élégance rencontre la mode.</p>
            <a href="{{ path('accueil') }}" class="btn">Découvrir</a>
        </div>
    </div>

    <!-- Right section for presentation or other content -->
    <div class="right-section">
        <div class="content-text">
           <h1>Bienvenue dans le monde du luxe</h1>
             <p>Découvrez nos dernières collections et défilés</p>
            <a href="{{ path('admin_blogs') }}" class="btn">Explorez le Blog</a>
        </div>
    </div>
</div>

<!-- Other content -->
<div class="container">
    {% block body %}{% endblock %}
</div>

{% block javascript %}{% endblock %}

</body>
</html>