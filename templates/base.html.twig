<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YM{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🤍</text></svg>">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <script src="https://kit.fontawesome.com/2bcd4f9bb1.js" crossorigin="anonymous"></script>
        <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ asset('css/Blog.css') }}">
    <link rel="stylesheet" href="{{ asset('css/blogcards.css') }}">
    <script src="https://kit.fontawesome.com/2bcd4f9bb1.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">

    {% block stylesheets %}
   <style>
/* Global styles */
body {
    font-family: 'Helvetica', sans-serif;
    background-color: #fafafa; /* Light cream background */
    color: #333; /* Dark grey text */
    margin: 0;
    padding: 0;
}

/* Navigation Bar */
.navbar {
    background-color: #ffffff; /* White background for a clean look */
    padding: 10px 30px; /* Adjusted padding for a more compact navbar */
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Soft shadow */
    border-bottom: 1px solid #e0e0e0; /* Subtle border */
    font-family: 'Montserrat', sans-serif;
    display: flex;
    justify-content: space-between; /* Distribute items with space */
    align-items: center;
}

/* Navbar Brand (Logo) */
.navbar-brand {
    font-size: 2.2rem;
    font-weight: 700;
    color: #2c3e50; /* Dark color for the brand */
    text-decoration: none;
    letter-spacing: 3px;
    text-transform: uppercase;
    font-family: 'Playfair Display', serif;
    transition: color 0.3s ease;
}

.navbar-brand:hover {
    color: #34495e; /* Hover color */
}

/* Navbar Menu */
.navbar-menu {
    display: flex;
    align-items: center;
}

.navbar-nav {
    list-style-type: none;
    display: flex;
    margin: 0;
    padding: 0;
}

.nav-item {
    margin-right: 20px; /* Adjust spacing between links */
    position: relative; /* Important for dropdown positioning */
}

.nav-link {
    color: #555555; /* Elegant grey color */
    font-size: 1.1rem;
    text-transform: uppercase;
    font-weight: 500;
    transition: color 0.3s ease, transform 0.3s ease;
    text-decoration: none;
    font-family: 'Montserrat', sans-serif;
}

.nav-link:hover {
    color: #2c3e50; /* Dark color on hover */
    transform: translateY(-3px); /* Elevation effect */
}

/* Dropdown Menu */
.nav-item.dropdown:hover .dropdown-menu {
    display: block; /* Make dropdown visible when hovered */
}

.nav-item.dropdown .dropdown-menu {
    display: none; /* Hide dropdown by default */
    background-color: #ffffff;
    border: 1px solid #ddd;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); /* Soft shadow */
    border-radius: 5px;
    padding: 10px 0;
    position: absolute; /* To position it properly */
    top: 100%;
    left: 0;
    min-width: 200px; /* Minimum width to prevent narrow menus */
    z-index: 1000; /* Ensure dropdown is on top of other elements */
}

/* Dropdown Item */
.nav-item.dropdown .dropdown-menu .dropdown-item {
    font-family: 'Montserrat', sans-serif;
    color: #555555;
    transition: background-color 0.3s ease;
}

.nav-item.dropdown .dropdown-menu .dropdown-item:hover {
    background-color: #f4f4f4; /* Soft hover color */
    color: #2c3e50; /* Dark color on hover */
}

/* Right-aligned Navbar Items (Login / Logout) */
.navbar-nav-right {
    display: flex;
    justify-content: flex-end; /* Align right */
    align-items: center; 
}

.navbar-nav-right .nav-item {
    margin-left: 20px; 
}

.btn {
    background-color: #F1f1f1; 
    color: #333;
    padding: 10px 20px;
    font-size: 1rem;
    font-weight: bold;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
}

.btn:hover {
    background-color: #d1c59a; 
    transform: translateY(-2px); 
}

/* Main content area */
.container {
    padding: 40px 20px;
    border-radius: 8px;
    margin-top: 100px; 
}

/* Alert styles */
.alert {
    border-radius: 5px;
    margin-bottom: 20px;
}


h1, h2, h3 {
    color: #f1f1f1;
    font-weight: bold;
}

</style>
    {% endblock %}
</head>
<body>
<nav class="navbar">
   <a href="{{ path('accueil') }}" class="navbar-brand">
        <img src="{{ asset('image.logo.png') }}" class="navbar-logo" alt="Logo yetm" width="50">
    </a>
    <div class="navbar-menu">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="{{ path('defiles') }}">Défilés</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_marque') }}">Marques</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('mannequins') }}">Mannequins</a></li>
            {# <li class="nav-item"><a class="nav-link" href="{{ path('blog') }}">Partie Blog</a></li> #}
            {% if app.user and is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#">Administration</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('admin_defiles') }}">Gestion des Défilés</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_marque') }}">Gestion des Marques</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_mannequins') }}">Gestion des Mannequins</a></li>
                    {# <li><a class="dropdown-item" href="{{ path('blog') }}">Gestion du blog</a></li> #}
                </ul>
            </li>
            {% endif %}
        </ul>
         <ul class="navbar-nav-right">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#">Mon Profil</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Se Connecter</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a></li>
                {% endif %}
        </ul>
    </div>
</nav>

<div class="container">
    {% for label, messages in app.flashes %}
    <div class="alert alert-{{ label }} mt-3">
        {% for message in messages %}
        <p>{{ message }}</p>
        {% endfor %}
    </div>
    {% endfor %}
    {% block body %}{% endblock %}
</div>

{% block javascript %}{% endblock %}

</body>
</html>
