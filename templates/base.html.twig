<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YM{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🤍</text></svg>">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <script src="https://kit.fontawesome.com/2bcd4f9bb1.js" crossorigin="anonymous"></script>

    {% block stylesheets %}
    <style>
        /* Global styles */
        body {
            font-family: 'Helvetica', sans-serif;
            background-color: ; /* Light cream background */
            color: #f1f1f1; /* Dark grey text */
            margin: 0;
            padding: 0;
        }

        /* Background image for the fashion show theme */
        .main-background {
            background-image: url('https://cdn.photoroom.com/v1/assets-cached.jpg?path=backgrounds_v3/white/Photoroom_white_background_extremely_fine_texture_only_white_co_d285fc74-d562-4a9f-b0b8-8cf7500231d8.jpg'); /* Replace with your image path */
            background-position: center;
            background-size: cover;
            background-attachment: fixed;
            height: 100vh;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
        }

        .intro-text {
            font-size: 2.5rem;
            font-weight: bold;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
        }

        .intro-text p {
            font-size: 1.2rem;
            font-weight: normal;
            margin-top: 10px;
        }

        /* Navigation Bar */
        .navbar {
            background-color: #fff; /* White background */
            padding: 15px 30px;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-size: 2rem;
            font-weight: bold;
            color: #222;
            text-decoration: none;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .navbar-nav {
            list-style-type: none;
            display: flex;
            margin-left: auto;
        }

        .nav-item {
            margin-right: 20px;
        }

        .nav-link {
            color: #555;
            font-size: 1.1rem;
            text-transform: uppercase;
            font-weight: 500;
            transition: color 0.3s ease, transform 0.3s ease;
            text-decoration: none;
        }

        .nav-link:hover {
            color: #333;
            transform: translateY(-2px); /* Elegant hover effect */
        }

        .nav-item.dropdown .dropdown-menu {
            background-color: #fff;
            border: 1px solid #ddd;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .dropdown-item {
            color: #f1f1f1;
            font-weight: 500;
        }

        .dropdown-item:hover {
            background-color: #F1f1f1; /* Soft cream color */
            color: #333;
        }

        .navbar-nav-right {
            display: flex;
            justify-content: flex-end;
        }

        .btn {
            background-color: #F1f1f1; /* Cream color */
            color: #333;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .btn:hover {
            background-color: #d1c59a; /* Slightly darker cream on hover */
            transform: translateY(-2px); /* Elegant hover effect */
        }

        /* Main content area */
        .container {
            padding: 40px 20px;
            border-radius: 8px;
            margin-top: 100px; /* Avoid content being hidden behind navbar */
        }

        /* Alert styles */
        .alert {
            border-radius: 5px;
            margin-bottom: 20px;
        }

        /* Styling for section headings */
        h1, h2, h3 {
            color: #f1f1f1;
            font-weight: bold;
        }

    </style>
    {% endblock %}
</head>
<body>
<!-- Navigation bar -->
<nav class="navbar">
    <a href="{{ path('accueil') }}" class="navbar-brand">
        <img src="{{ asset('images.jpg/logo.png') }}" src="" class="navbar-logo">
    </a
    <div class="navbar-menu">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_defiles') }}">Défilés</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_marque') }}">Marques</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_mannequins') }}">Mannequins</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('admin_blog') }}">Partie Blog</a></li>
            {% if app.user and is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#">Administration</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('admin_defiles') }}">Gestion des Défilés</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_marque') }}">Gestion des Marques</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_mannequins') }}">Gestion des Mannequins</a></li>
                    <li><a class="dropdown-item" href="{{ path('admin_blog') }}">Gestion du blog</a></li>
                </ul>
            </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav-right">
            {% if app.user %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#"> {{ app.user.fullName }}</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">Mon profil</a></li>
                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                </ul>
            </li>
            {% else %}
            <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Se Connecter</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a></li>
            {% endif %}
        </ul>
    </div>
</nav>

<!-- Main content area -->
<div class="container">
    {% for label, messages in app.flashes %}
    <div class="alert alert-{{ label }} mt-3">
        {% for message in messages %}
        <p>{{ message }}</p>
        {% endfor %}
    </div>
    {% endfor %}
    {% block body %}{% endblock %}
</div>

{% block javascript %}{% endblock %}

</body>
</html>